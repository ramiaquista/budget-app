<h1>Transactions</h1>
<h2>Total Amount in this category: <%= @total %></h2>
<h4>Most Recents</h4>


<div id="expenses">
  <% @expenses.each do |expense| %>
    <%= render expense %>
    <p>
      <%= link_to "Show this expense", category_expense_path(:id => expense.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New expense", new_category_expense_path %>
